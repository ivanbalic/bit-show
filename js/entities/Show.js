class Show {
    constructor(title, details, images, id) {
        this.title = title;
        this.details = details;
        this.images = images;
        this.id = id;
    }
}

export default Show;

